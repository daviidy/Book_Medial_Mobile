part of buy_echec_box_view;

class _BuyEchecBoxTablet extends StatelessWidget {
  final BuyEchecBoxViewModel viewModel;

  _BuyEchecBoxTablet(this.viewModel);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('BuyEchecBoxTablet')),
    );
  }
}